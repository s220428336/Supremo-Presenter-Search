@model IEnumerable<Supremo_Presenter_Search.Models.VotesModel>

@{
    ViewData["Title"] = "Index";
}
<p>presents...</p>
<h2>SUPREMO PRESENTER SEARCH</h2>
<img src="#" width="100%" height="75%" alt="presenter search" />

<h3>VOTING BUNDLES</h3>

<p>
    <a asp-action="Create">Create New</a>
</p>
<div class="Bundles">
    @if(Model.Count() > 0)
    {
    <table>
        <thead>
            <tr></tr>
        </thead>
        <tbody>
            @foreach(var item in Model)
            {
            <tr>
                <td>
                    <div class="card">
                        <div class="card-body">
                                    <img src="" /><span>5 VOTES<br />R10.00</span><span><button><i>❤️</i></button><button>Add to Cart</button></span>
                        </div>
                    </div>
                </td>
            </tr>
            }

        </tbody>
    </table>
    }
    
</div>